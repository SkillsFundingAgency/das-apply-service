@model SFA.DAS.ApplyService.Web.ViewModels.SubmittedViewModel
@{
    ViewBag.Title = "Not Submitted";
    Layout = "_Layout";
}

<main class="govuk-main-wrapper govuk-main-wrapper--l" id="main-content" role="main">
    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">

            <div class="govuk-panel govuk-panel--confirmation">
                <h1 class="govuk-panel__title">Not Submitted</h1>
                <div class="govuk-panel__body">
                    <div class="govuk-!-margin-bottom-4">
                        <span class="govuk-!-font-size-24">There is another application which has been submitted. We cannot progress yours at this time.</span>
                    </div>
                    @if (!string.IsNullOrEmpty(Model.ReferenceNumber))
                    {
                    <div>
                        <span class="govuk-!-font-size-24">Your reference number</span>
                        <br />
                        <strong>@Model.ReferenceNumber</strong>
                    </div>
                    }
                </div>
            </div>

            <h3 class="govuk-heading-m">What happens next</h3>

            <p class="govuk-body">Please check with the other applicant.</p>
            <p class="govuk-body">Contact us if you have received this message in error.</p>

            <p class="govuk-body">
                <a href="@Model.FeedbackUrl" target="_blank" class="govuk-link">What did you think of this service?</a>
            </p>
            @* LINK BELOW REMOVED FOR PRIVATE BETA *@
            @*
                <p class="govuk-body">
                    <a asp-action="Applications" asp-controller="Application" class="govuk-link">Return to application overview</a>
                </p>
            *@
        </div>
    </div>
</main>