@model SFA.DAS.ApplyService.Web.ViewModels.Roatp.ChangeUkprnViewModel

@{
    var title = $"Changing your UKPRN";
    ViewBag.Title = title;
    Layout = "_Layout";
}

@{ await Html.RenderPartialAsync("~/Views/Roatp/ApplicationHeader.cshtml"); }

@*<div class="grid-row">
    <form asp-controller="RoatpApplication" asp-action="TaskList" method="get" name="backForm">
        @Html.HiddenFor(x => x.ApplicationId)
        <a href="JavaScript:document.backForm.submit();" class="govuk-back-link">Back</a>
    </form>
</div>*@

<div class="govuk-width-container">

    <main class="govuk-main-wrapper " id="main-content" role="main">

        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds">
                <h1 class="govuk-heading-l">@title</h1>
                <p class="govuk-body">
                    If you change your UKPRN, you may be required to answer the following application questions again. This is because the information we get from UKRLP determines which application questions we ask you.
                </p>
                <p class="govuk-body">
                    If you do not provide the correct UKPRN, your application may be rejected.
                </p>
                
                <form method="post" asp-action="ConfirmChangeUkprn" asp-controller="RoatpApplicationPreamble">                      
                    @Html.HiddenFor(x => x.ApplicationId)
                    <div class="govuk-grid-row">
                        <div class="govuk-grid-column-two-thirds">
                            <input type="submit" class="govuk-button" value="Continue" />
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </main>
</div>