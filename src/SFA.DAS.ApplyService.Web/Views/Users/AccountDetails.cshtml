@using SFA.DAS.ApplyService.Web.Constants
@model SFA.DAS.ApplyService.Web.ViewModels.StubAuthenticationViewModel
@{
    Layout = "_Layout";
}
<h1 class="govuk-heading-xl">Stub Authentication - Enter sign in details from Contacts table</h1>
<p class="govuk-body">If you enter a Id (GUID) format and a new email address a new user will be created</p>
<div class="govuk-grid-row">
    <div class="govuk-grid-column-one-half">
        <form method="POST" asp-route="@RouteNames.StubAccountDetailsPost">
            <input asp-for="ReturnUrl" type="hidden" />
            <div class="govuk-form-group">
                <label class="govuk-label" asp-for="Id">
                    Sign In ID
                </label>
                <input class="govuk-input" asp-for="Id" type="text">
            </div>
            <div class="govuk-form-group">
                <label class="govuk-label" asp-for="Email">
                    Email
                </label>
                <input class="govuk-input" asp-for="Email" type="email">
            </div>
            <div class="govuk-button-group">
                <button class="govuk-button" type="submit">Sign in</button>
            </div>
        </form>
    </div>
</div>